# LIYT Backend

Standalone backend repo for LIYT. Rails 8 API app lives in `apps/liyt_api`.

## Layout

- `apps/liyt_api` - Rails API app
- `domains` - domain modules (plain Ruby)
- `infra` - shared infrastructure helpers
- `docs` - product/architecture docs
