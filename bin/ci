#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
APP_DIR="$ROOT_DIR/apps/liyt_api"

cd "$APP_DIR"

bundle install
bin/rails db:prepare
bin/rails test
